# QB-WebUI 开发计划

## 当前进度 (2024-03-14)

### ✅ 已完成的功能

1. **登录页面**
   - 简化的登录表单（仅用户名和密码）
   - 默认连接到 localhost:8088
   - 登录成功后跳转到主界面
   - Cookie (SID) 自动保存到 localStorage

2. **路由和认证**
   - Vue Router 配置
   - 导航守卫检查认证状态
   - 403 错误处理和自动跳转到登录页
   - 修复了登录页面的重定向循环问题

3. **API 代理配置**
   - Vite 代理配置：所有 `/api/*` 请求转发到 `localhost:8088`
   - API 客户端强制使用代理模式

4. **主界面布局**
   - 侧边栏菜单（任务列表、分类、标签）
   - 顶部导航栏（添加任务、登出按钮）
   - 条件渲染：登录页面不显示主布局

### 🔄 进行中的功能

1. **种子列表页面 (Dashboard.vue)**
   - 显示种子列表
   - 支持筛选（全部、下载中、已完成、暂停、做种）
   - 种子操作（暂停、恢复、删除）

2. **设置页面 (Settings.vue)**
   - 显示和修改 qBittorrent 设置

### 📋 待开发功能

1. **种子详情页面**
   - 查看种子详细信息
   - 文件列表
   -  trackers 信息

2. **添加种子功能**
   - 通过 URL 或磁力链接添加种子
   - 选择保存路径、分类等选项

3. **分类和标签管理**
   - 创建/删除分类
   - 创建/删除标签
   - 为种子添加/移除标签

4. **速度限制管理**
   - 全局速度限制
   - 单个种子速度限制

5. **UI 美化**
   - 改进视觉层次
   - 增加标题层级
   - 优化颜色方案

## 明天需要继续的工作 (2024-03-15)

### 优先级 1: 核心功能

1. **完成种子列表页面**
   - 实现种子数据的获取和显示
   - 添加筛选功能（按状态筛选）
   - 实现种子操作按钮（暂停、恢复、删除）

2. **完善登录流程**
   - 测试 Cookie 持久化
   - 测试 403 错误处理
   - 测试登出功能

### 优先级 2: 用户体验

1. **添加种子功能**
   - 创建添加种子的模态框
   - 实现通过 URL 添加种子
   - 支持选项配置（保存路径、分类、暂停等）

2. **UI 改进**
   - 优化侧边栏样式
   - 改进表格样式
   - 增加加载状态提示

### 优先级 3: 高级功能

1. **分类和标签管理**
   - 实现分类列表和操作
   - 实现标签列表和操作
   - 为种子添加/移除标签

2. **设置页面**
   - 显示当前设置
   - 修改设置功能

## 技术债务

1. **API 错误处理**
   - 需要统一的错误处理机制
   - 网络错误提示

2. **代码优化**
   - 提取公共组件
   - 优化 API 调用逻辑

## 注意事项

1. 所有 `/api/v2` 接口访问都需要通过代理中转到本机的 8088 端口
2. 登录页面不需要输入后台地址和端口，默认使用 localhost:8088
3. Cookie (SID) 会自动保存到 localStorage，登录状态会持久化
4. 403 错误会自动跳转到登录页面
